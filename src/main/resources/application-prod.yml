spring:
  application:
    name: timeToast

  h2:
    console:
      enabled: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/prod_timetoast
    username: developer
    password: TimeToast2@

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  management:
    endpoint:
      health:
        probes:
          enabled: true
    health:
      livenessState:
        enabled: true
      readinessState:
        enabled: true

  profiles:
    include: oauth

  jwt:
    key: b06a03df1e61f3347288ad580a55904f3e7c2367179d5727e779b3ef63207bc8