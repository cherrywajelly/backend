spring:
  application:
    name: timeToast

  config:
    activate:
      on-profile: local

  h2:
    console:
      enabled: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://138.2.124.99:3306/test_timetoast
    username: root
    password: password

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    open-in-view: false

  #  sql:
  #    init:
  #      mode: embedded

  management:
    endpoint:
      health:
        probes:
          enabled: true
    health:
      livenessState:
        enabled: true
      readinessState:
        enabled: true


  jwt:
    key: b06a03df1e61f3347288ad580a55904f3e7c2367179d5727e779b3ef63207bc8