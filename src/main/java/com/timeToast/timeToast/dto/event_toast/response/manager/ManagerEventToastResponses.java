package com.timeToast.timeToast.dto.event_toast.response.manager;

import java.util.List;

public record ManagerEventToastResponses(
        List<ManagerEventToastResponse> managerEventToastResponses
) {
}
